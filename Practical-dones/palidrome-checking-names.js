const name = "shifan"
let longs = ''
for( let i = name.length -1 ; i >= 0 ; i--){
      longs =  longs + name[i]
         result = false
      if ( result = (name == longs)){
        result = true
    } 
    else if(!result) {    
    }
}
console.log( !result, " : it's not a palidrome");
console.log( result , " :  its palidrome " ); 
console.log(longs);
